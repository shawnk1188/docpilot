```

Now fill in the three files that matter for this commit — `.gitignore`, `README.md`, and `Makefile`. These are what a recruiter sees first when they land on the repo.

**`.gitignore`:**
```
__pycache__/
*.pyc
.venv/
venv/
.env
*.egg-info/
.pytest_cache/
.mypy_cache/
dist/
build/
docs/sample/*.pdf
notebooks/.ipynb_checkpoints/
